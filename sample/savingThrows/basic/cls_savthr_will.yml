# Baseline from which all other high-Will progressions are derived.
yamlType: cls_savthr
generateOutput: true

# Default other saves to low progression.
# Note that levelProgression and levelList schemas
# use `inherits` logic, not `imports`!
inherits:
  - highWill
  - cls_savthr_low